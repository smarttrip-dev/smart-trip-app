{
  "name": "smart-trip",
  "version": "1.0.0",
  "description": "A constraint-based itinerary optimization system.",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend"
  },
  "keywords": [
    "mern",
    "react",
    "nodejs",
    "travel"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "puppeteer": "^24.37.5"
  }
}
